# Online course backend (API). Dev: Vite proxies /api here. Prod: full URL to your API.
VITE_ONLINE_COURSE_API=http://localhost:8000

# VTS admin panel (staff site). Use in UI when linking to the dashboard.
VITE_ADMIN_APP=http://localhost:5174
